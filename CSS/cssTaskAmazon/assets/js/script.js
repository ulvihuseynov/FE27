// function truncateText(element, maxLength) {
//     if (element.innerText.length > maxLength) {
//         element.innerText = element.innerText.substring(0, maxLength) + "...";
//     }
// }

// const cards = document.querySelectorAll('.news-info');
// cards.forEach(card => truncateText(card,50)); // 100 burada mətnin uzunluğunu təyin edir
